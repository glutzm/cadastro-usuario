# Spring Data
spring:
  datasource:
    url: ${DATABASE_URL}

  messages:
    basename: languages/messages

  thymeleaf:
    cache: true

  data:
    web:
      pageable:
        one-indexed-parameters: true
